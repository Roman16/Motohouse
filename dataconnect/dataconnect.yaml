specVersion: "v1beta"
serviceId: "motohouse"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "motohouse-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
